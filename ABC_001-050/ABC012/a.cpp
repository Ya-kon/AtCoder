#include <iostream>
#define rep(i,n) for (int i = 0; i < (n); ++i)
using namespace std;
using ll = long long;

int main() {

    int A, B;
    cin >> A >> B;

    swap(A, B);

    cout << A << " " << B << endl;
   
    return 0;
}