#include <iostream>
#define rep(i,n) for (int i = 0; i < (n); ++i)
using namespace std;
using ll = long long;

int main() {
    int H1, H2;
    cin >> H1 >> H2;
    
    int ans = H1 - H2;
    cout << ans << endl;
    return 0;
}